# Local dev: leave unset to use JSON files under server/data/

# Vercel / production: create a Redis database via Vercel Marketplace (Upstash)
# and connect it to the project — these are injected automatically, or paste here for local testing:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
